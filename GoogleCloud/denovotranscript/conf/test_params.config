params { 
    input = 'test_samplesheet.csv'
    outdir = 'gs://<YOUR-BUCKET-NAME>/outdir_transcriptome/'
    workDir = 'gs://<YOUR-BUCKET-NAME>/work_transcriptome/'
    // max_memory = '6.GB'
    // max_cpus = 2
    // max_time = '6.h'
    // Add other parameters as needed 
    
    // fastp parameters 
    adapter_fasta = null
    discard_trimmed_pass = false
    save_trimmed_fail = false
    save_merged = false
    
    // sortmerna params
    skip_rrna_filter = true
    rRNA_database = null  // Path to your rRNA database
}